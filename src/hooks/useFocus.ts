// import { ReactNode, useRef, useEffect } from 'react';

// export const useFocus = (area: ReactNode) => {
//     const areaRef = useRef([]);

//     useEffect(() => {
//         areaRef.current = areaRef.current.slice(0, area.length)
//     },[])
// }